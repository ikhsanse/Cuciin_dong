<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gir.cuciindong">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PilihUser">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginMitra" />
        <activity android:name=".RegisterMitra1" />
        <activity android:name=".register_mitra2" />
        <activity android:name=".HomeMitra" />
        <activity android:name=".HomeCustomer" />
        <activity android:name=".InformasiLaundry"></activity>
    </application>

</manifest>